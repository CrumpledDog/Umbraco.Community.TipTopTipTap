{
  "name": "umbraco-community-tiptoptiptap-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install-browsers": "playwright install --with-deps chromium",
    "restore-seed-db": "node scripts/restore-seed-db.mjs",
    "build-seed-db": "node scripts/build-seed-db.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  }
}
